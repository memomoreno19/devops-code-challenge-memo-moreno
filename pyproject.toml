[project]
name = "devops-code-challenge"
version = "0.1.0"
description = "Sample FastAPI project for DevOps validation"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "httpx",
    "uvicorn",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]